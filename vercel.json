{
  "buildCommand": "cd grow-frontend && npm install --legacy-peer-deps && CI=false npm run build",
  "outputDirectory": "grow-frontend/build",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "create-react-app",
  "rewrites": [
    { "source": "/(.*)", "destination": "/grow-frontend/build/index.html" }
  ]
}
